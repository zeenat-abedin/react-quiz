import React from "react";

export default function StartScreen() {
  return (
    <div className="">
      <h2>Welcome to the React Quiz!</h2>
      <h3>X questions to test your React Mastery</h3>
      <button onClick={() => {}} id="start-btn" disabled>
        Let's start
      </button>
    </div>
  );
}
